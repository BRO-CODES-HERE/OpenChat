design a system for chat application in terminal over SSH which get connect to another computer with its IP (world wide , anywhere  ) when both systems open our appication and puts the IP 


things are IMP
---> users gets a option to store msg in their local system or erase forever 
---> encryption and E2E commutication 
---> no one can read the msgs in between 
---> (Room Chat ) user can create a public room to chat in group (with over one ID )

create a ChatSSH.md file 
include all nessesecry Tech stack and flow of building this